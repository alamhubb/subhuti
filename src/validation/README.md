# Subhuti Grammar Validation - 架构说明

## 📋 概述

Subhuti语法验证系统用于**静态分析**Parser的语法规则，检测Or分支顺序问题。

## 🏗️ 架构

```
┌─────────────────────────────────────────────────────────────┐
│                    SubhutiGrammarValidator                   │
│                      （验证器入口）                           │
└─────────────────────────────────────────────────────────────┘
                              ↓
        ┌─────────────────────┴─────────────────────┐
        ↓                                           ↓
┌──────────────────────┐                 ┌──────────────────────┐
│ SubhutiRuleCollector │                 │SubhutiConflictDetector│
│   （规则收集器）      │                 │   （冲突检测器）      │
│                      │                 │                      │
│ 功能：收集AST        │                 │ 功能：检测Or分支冲突  │
│ 方法：双层Proxy拦截  │                 │ 方法：前缀检测        │
└──────────────────────┘                 └──────────────────────┘
        ↓                                           ↓
        │                                 ┌──────────────────────┐
        │                                 │SubhutiGrammarAnalyzer │
        │                                 │  （语法分析器）       │
        │                                 │                      │
        │                                 │ 功能：计算路径        │
        │                                 │ 方法：展开subrule     │
        │                                 └──────────────────────┘
        │                                           ↑
        └───────────────────────────────────────────┘
                        提供AST
```

## 🔧 核心组件

### 1. SubhutiRuleCollector（规则收集器）

**功能**：收集Parser中所有规则的AST结构

**实现方案**：双层Proxy拦截

- **Parser Proxy**：拦截Or/Many/Option/AtLeastOne/子规则调用
- **TokenConsumer Proxy**：拦截token消费调用

**为什么需要双层Proxy？**
- tokenConsumer是独立对象，不是Parser的方法
- 规则内部通过`this.tokenConsumer.XXX()`消费token
- 单层Proxy无法拦截tokenConsumer的方法调用

**收集到的AST节点类型**：
- `consume`: token消费（如`LParen`, `RParen`）
- `subrule`: 子规则调用（如`Arguments`, `Identifier`）
- `or`: Or分支
- `sequence`: 序列
- `option`: 可选
- `many`: 0次或多次
- `atLeastOne`: 至少1次

---

### 2. SubhutiGrammarAnalyzer（语法分析器）

**功能**：计算规则的所有可能路径（token序列）

**实现方案**：方案A - 完全展开subrule

**核心原理**：
1. **展开subrule**：遇到subrule节点时，递归计算该规则的所有可能token序列
   - 不是记录subrule名称（如`'Arguments'`）
   - 而是展开为实际的token路径（如`'LParen,Identifier,RParen,'`）

2. **笛卡尔积拼接**：对于sequence节点，计算所有子节点路径的笛卡尔积
   - 示例：`['a,'] × ['b,', 'c,'] = ['a,b,', 'a,c,']`

3. **路径格式**：使用逗号分隔的字符串表示token序列
   - 示例：`'LParen,RParen,'` 表示 `LParen → RParen`
   - 优点：可以直接使用字符串的`startsWith`检测前缀关系

**性能优化**：
- 缓存机制：每个规则的路径只计算一次
- 路径数量限制：默认10000条（防止路径爆炸）
- 路径长度限制：默认1000个token
- 渐进式终止：达到限制立即停止

---

### 3. SubhutiConflictDetector（冲突检测器）

**功能**：检测Or规则中的路径冲突

**实现方案**：方案A - 基于完全展开的token路径进行前缀检测

**检测级别**：

#### Level 1: 空路径检测（FATAL）
- **检测**：分支是否可以匹配空（路径包含`''`）
- **问题**：如果前面的分支可以匹配空，后续所有分支都不可达
- **示例**：`Or([Option(A), B])` → `Option(A)`可以匹配空，`B`永远不会被尝试

#### Level 2: 前缀冲突检测（ERROR）
- **检测**：前面分支的路径是否是后面分支路径的前缀
- **方法**：使用字符串的`startsWith`检测
- **示例**：
  ```
  分支A路径：'=,'
  分支B路径：'==,'
  '==,'.startsWith('=,') → true，有冲突！
  ```
- **原因**：PEG是贪婪匹配，分支A会先匹配`'='`，导致分支B的`'=='`无法完整匹配

---

## 🎯 完整流程

```
1. SubhutiRuleCollector.collectRules(parser)
   ↓
   收集所有规则的AST
   {
     'Arguments': { type: 'or', alternatives: [...] },
     'Identifier': { type: 'consume', tokenName: 'Identifier' },
     ...
   }

2. SubhutiGrammarAnalyzer.computePaths('Arguments')
   ↓
   展开subrule，计算所有可能的token路径
   [
     'LParen,RParen,',
     'LParen,Identifier,RParen,',
     'LParen,Identifier,Comma,Identifier,RParen,',
     ...
   ]

3. SubhutiConflictDetector.detectOrConflicts(...)
   ↓
   检测Or分支的前缀冲突
   - 计算每个分支的路径
   - 两两比较，检测前缀关系
   - 生成错误报告

4. SubhutiGrammarValidator.validate()
   ↓
   输出验证结果
   - 错误列表
   - 警告列表
   - 统计信息
```

---

## 💡 关键设计决策

### 为什么选择方案A（展开subrule）？

**优点**：
- ✅ 准确：能检测到实际的token序列冲突
- ✅ 直观：路径是可读的token序列
- ✅ 完整：subrule被完全展开

**缺点**：
- ⚠️ 路径爆炸：复杂规则可能产生大量路径
  - 解决方案：限制路径数量（10000条）
- ⚠️ 递归规则：可能导致无限展开
  - 解决方案：缓存机制
- ⚠️ 性能开销：需要计算大量路径
  - 解决方案：只在开发环境运行

### 为什么使用字符串的startsWith？

- ✅ 简单高效
- ✅ 直观易懂
- ✅ 符合PEG的语义（贪婪匹配 + 有序选择）

---

## 📝 使用示例

```typescript
import { SubhutiGrammarValidator } from './SubhutiGrammarValidator'
import Es2025Parser from '../language/es2025/Es2025Parser'

// 创建验证器
const validator = new SubhutiGrammarValidator()

// 验证语法
const errors = validator.validate(Es2025Parser)

// 输出结果
if (errors.length > 0) {
    console.log('发现语法问题：')
    errors.forEach(error => {
        console.log(`[${error.level}] ${error.message}`)
        console.log(`  建议：${error.suggestion}`)
    })
}
```

---

## 🔄 版本历史

- **v1.0.0**: 初始版本，基本的AST收集和前缀检测
- **v2.0.0**: 改进AST收集器，使用双层Proxy，能够正确收集consume和subrule节点
- **v2.1.0**: 新增First集合计算，支持两种模式切换

---

## 🔄 **切换检测模式**

### 两种模式

#### 模式1：完全展开（默认）

**特点**：
- ✅ 精确检测
- ✅ 能发现所有前缀冲突
- ⚠️ 可能路径爆炸（复杂规则）

**适用场景**：
- 简单规则
- 需要精确检测

#### 模式2：First集合

**特点**：
- ✅ 快速，不会路径爆炸
- ✅ 内存占用小
- ⚠️ 不够精确（可能误报或漏报）

**适用场景**：
- 复杂规则
- 快速检查

---

### 如何切换

打开文件：`subhuti/src/validation/SubhutiGrammarAnalyzer.ts`

找到`computePaths`方法（约第106行），修改第138-145行：

```typescript
// 3. 递归计算路径
// ============================================
// 🔄 切换点：在这里切换使用哪种模式
// ============================================

// 模式1：完全展开（精确但可能路径爆炸）
const paths = this.computeNodePaths(ruleNode)

// 模式2：First集合（快速但不够精确）
// const firstSet = this.computeNodeFirst(ruleNode)
// const paths = this.convertFirstSetToPaths(firstSet)

// ============================================
```

**切换到First集合模式**：

```typescript
// 模式1：完全展开（精确但可能路径爆炸）
// const paths = this.computeNodePaths(ruleNode)

// 模式2：First集合（快速但不够精确）
const firstSet = this.computeNodeFirst(ruleNode)
const paths = this.convertFirstSetToPaths(firstSet)
```

就这么简单！只需要注释/取消注释对应的行即可。

---

### 对比示例

#### 完全展开模式

```typescript
// Arguments规则
Arguments: Or([
    LParen + RParen,
    LParen + ArgumentList + RParen
])

// 路径（完全展开）
分支#0: ['LParen,RParen,']
分支#1: ['LParen,Identifier,RParen,', 'LParen,Ellipsis,Identifier,RParen,', ...]
```

#### First集合模式

```typescript
// Arguments规则
Arguments: Or([
    LParen + RParen,
    LParen + ArgumentList + RParen
])

// 路径（First集合）
分支#0: ['LParen,']
分支#1: ['LParen,']

// 检测结果：两个分支的First都是'LParen'，有冲突（误报）
```

**注意**：First集合模式可能会误报，因为它只看第一个token，不看完整路径。

---

## 🏗️ 重构后的模块化架构

### 新的文件结构

经过重构，原本 3269 行的巨型文件已拆分为模块化结构：

```
validation/
├── README.md                       # 唯一文档
├── index.ts                        # 统一导出
│
├── types/                          # 类型定义
│   └── SubhutiValidationError.ts   # 错误类型定义 (10KB)
│
├── analyzers/                      # 分析器核心
│   ├── SubhutiGrammarAnalyzer.ts   # 门面类 (6.4KB，重写)
│   ├── SubhutiGrammarValidator.ts  # 验证器入口 (2.2KB)
│   └── SubhutiRuleCollector.ts     # 规则收集器 (18KB)
│
├── detectors/                      # 检测器
│   ├── LeftRecursionDetector.ts    # 左递归检测器 (9KB)
│   └── OrConflictDetector.ts       # Or冲突检测器 (8KB)
│
├── core/                           # 路径展开
│   ├── PathExpander.ts             # DFS路径展开器 (14KB)
│   └── BFSPathExpander.ts          # BFS路径展开器 (9.4KB)
│
├── utils/                          # 工具类
│   ├── PerformanceAnalyzer.ts      # 性能分析器 (14KB)
│   └── PathUtils.ts                # 路径工具函数 (3.3KB)
│
├── debug/                          # 调试工具
│   └── SubhutiValidationDebugger.ts  # 调试器 (23KB)
│
└── constants/                      # 常量
    └── ExpansionLimits.ts          # 配置常量 (1.1KB)
```

### 代码精简对比

| 分类 | 旧代码 | 新代码 | 变化 |
|------|--------|--------|------|
| 核心功能 | 1500行 | 1300行 | -200 (去除日志) |
| 调试/日志 | 300行 | 0行 | -300 |
| 深度计算 | 200行 | 0行 | -200 |
| 图分析 | 60行 | 0行 | -60 |
| 前缀树 | 50行 | 0行 | -50 |
| 复杂检测 | 260行 | 0行 | -260 (简化) |
| 统计分析 | 50行 | 0行 | -50 |
| **新增工具** | 0行 | 560行 | +560 (模块化) |
| **总计** | **3269行** | **1860行** | **-1409行 (-43%)** |

### 删除的功能

#### ✂️ 调试功能（~300行）
- 日志系统（文件日志、控制台日志）
- 深度计算和统计
- Mermaid 图表生成

#### ✂️ 重复功能（~320行）
- **图分析** - 与 DFS 左递归检测功能重复
- **多种冲突检测实现** - 简化为 First(K) 检测

#### ✂️ 高级检测（~50行）
- **前缀树检测** - 简化为使用 Set 进行完全匹配
- **ArrayTrie 类** - 不再需要

#### ✂️ 辅助工具（~730行）
- 规则可能性计算
- 深度统计
- 各种备用实现

### 保留的核心功能

✅ **DFS/BFS 路径展开** - 完整保留  
✅ **左递归检测** - 完整保留  
✅ **Or 冲突检测 (First(K))** - 保留主流程，简化为相等检测  
✅ **所有节点类型展开** - Or, Sequence, Option, Many, AtLeastOne  
✅ **笛卡尔积计算** - 完整保留优化版本  
✅ **性能分析** - 独立为 PerformanceAnalyzer 模块

### 架构优势

1. **单一职责** - 每个类只负责一个功能
2. **模块化** - 清晰的依赖关系，可独立测试
3. **可扩展性** - 添加新检测器无需修改现有代码
4. **性能优化** - 检测器共享缓存，避免重复计算
5. **易维护** - 代码量减少 43%，结构更清晰

### 使用方式

#### 基本使用（API 保持兼容）

```typescript
import { SubhutiGrammarValidator } from './validation'

// 验证语法（与之前完全相同）
SubhutiGrammarValidator.validate(parser)
```

#### 高级使用（使用新的模块化组件）

```typescript
import { 
    SubhutiGrammarAnalyzer,
    LeftRecursionDetector,
    OrConflictDetector 
} from './validation'

// 创建分析器
const analyzer = new SubhutiGrammarAnalyzer(ruleASTs, tokenCache)

// 执行验证
const { errors, stats } = analyzer.initCacheAndCheckLeftRecursion()

// 或者单独使用检测器
const leftRecDetector = new LeftRecursionDetector(ruleASTs, tokenCache, perfAnalyzer)
const { errors } = leftRecDetector.detect()
```

---

## 📌 重要说明

- ✅ 所有公开 API 保持向后兼容
- ✅ 现有代码无需修改
- ✅ 共享缓存机制提升性能
- ✅ 错误类型和格式保持不变

